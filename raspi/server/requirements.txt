# Web Framework
fastapi==0.115.*
uvicorn[standard]==0.34.*
python-dotenv==1.1.*

# Async
aiofiles==24.*
websockets==13.*

# Computer Vision
opencv-python-headless==4.10.*
# numpy must stay <2: HailoRT 4.23 (pip package `hailort`) requires numpy<2.
# 1.26 works for both engines (ONNX Runtime 1.20 supports it on Python 3.11+).
numpy>=1.26,<2
Pillow==11.*

# Inference (CPU fallback)
onnxruntime==1.20.*

# Hardware
pyserial==3.5
RPi.GPIO==0.7.1

# Utilities
pydantic==2.10.*
pydantic-settings==2.7.*
