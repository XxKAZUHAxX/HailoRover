# ext/CMakeLists.txt — vendored third-party code (in-tree, no submodules)
# Purpose: adds vendor driver and middleware sources to ${PROJECT_NAME}.

add_subdirectory(drivers)
add_subdirectory(middlewares)
